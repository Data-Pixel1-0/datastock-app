name: datastock_mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  mobile_scanner: ^7.0.1
  qr_flutter: ^4.1.0
  http: ^1.5.0
  firebase_core: ^4.10.0
  firebase_database: ^12.4.2
  firebase_auth: ^6.5.2
  mysql1: ^0.20.0
  shared_preferences: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/logo.jpg
    - lib/assets/logo.png
